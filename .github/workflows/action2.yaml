name: Action2

run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment Environment'
        type: choice
        default: 'Env-Q'
        options:
        - Env-A
        - Env-Q
        - Env-Z

jobs:
  Actions2-Job1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Actions2-Job1"
      - run: echo ${{ inputs.environment }}
      - run: echo ${{ secrets.ENV_NAME }}
